import type { IndustryType } from "@/domain/vendor/vendor-types";

/** Demo storefront slugs mapped to industry for browse filters. */
export const staticVendorIndustryTypes: Record<string, IndustryType> = {
  "mandawee-market": "FOOD_BEVERAGES",
  "fresh-farm-co": "AGRICULTURE",
};
